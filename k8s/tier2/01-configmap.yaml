apiVersion: v1
kind: ConfigMap
metadata:
  name: tier2-config
  namespace: loan-app
data:
  TIER3_URL: "http://tier3-service.loan-app.svc.cluster.local:8000"
  SERVER_PORT: "8080"
  SPRING_PROFILES_ACTIVE: "production"
